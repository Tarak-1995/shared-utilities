apiVersion: v2
kind: Service
metadata:
  name: sharedutilitiesservice
  labels:
    app: sharedutilitiesservice
    version: "v2"
spec:
  ports:
  - port: 80
    targetPort: 5000
initialDelaySeconds: 120    
  selector:
    app: sharedutilities
    version: "v2"
