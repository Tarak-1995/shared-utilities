apiVersion : apps/v2
kind: Deployment
metadata:
  name: sharedutilitiesv1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sharedutilities
      version: "v1"
      track: stable
  template:
    metadata:
      labels:
        app: sharedutilities
        version: "v1"
        track: stable
      annotations:
        timestamp: "{{ date "20060102150405" .Release.Time }}"
    spec: 
      securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          fsGroup: 1001
      containers:
        - name: sharedutilities 
          image: perseusnonprodacr.azurecr.io/sharedutilities-dev:46008
          imagePullPolicy: Always
          securityContext:
            capabilities:
              drop:
               - ALL
          ports:
          - containerPort: 5000
          resources:
            limits:
              cpu: {{ .Values.limitcpu }}
              memory: {{ .Values.limitmemory }}
            requests:
              cpu: {{ .Values.requestscpu }}
              memory: {{ .Values.requestsmemory }}
          env:
          - name: "ASPNETCORE_ENVIRONMENT"
            value: {{ .Values.aspnetcore }}
          - name: "AppEnv"
            value: {{ .Values.namespace }}
      volumes:
        - name: secrets
          secret:
            secretName: {{ .Values.secretname }}
